# Optional ML0 research environment.  The normal historical-data/frontend
# pipeline does not install or import these dependencies.
numpy>=1.24
scipy>=1.10

# Optional accelerator/model replacement for a future ML0 rerun.  The current
# repository builder uses its deterministic NumPy R-GCN-style fallback when
# PyTorch/PyG are unavailable.
# torch>=2.0
# torch-geometric>=2.4
